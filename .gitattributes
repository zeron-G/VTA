# Normalize all text files to LF in the repo (project runs on WSL/Linux/CI).
* text=auto eol=lf

# Lockfiles & generated
pnpm-lock.yaml linguist-generated=true

# Binary assets (never normalize)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
